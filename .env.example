# Public Data Portal API key (data.go.kr)
# Obtain from https://www.data.go.kr and paste below
DATA_GO_KR_API_KEY=your_api_key_here

# Optional overrides:
# - odcloud Applyhome (청약홈): by default this project reuses DATA_GO_KR_API_KEY.
#   You can override with either an Authorization header key or a serviceKey query param.
# ODCLOUD_API_KEY=your_odcloud_api_key_here
# ODCLOUD_SERVICE_KEY=your_odcloud_service_key_here
#
# - Onbid next-gen (B010003): by default this project reuses DATA_GO_KR_API_KEY.
# ONBID_API_KEY=your_onbid_api_key_here

# TLS options for corporate proxy / SSL inspection environments:
# - Prefer setting a CA bundle that includes your corporate root/intermediate certs.
# REAL_ESTATE_SSL_CA_BUNDLE=/path/to/corporate-ca-bundle.pem
# - On macOS, automatically build a CA bundle from System/login keychains (default: enabled).
# REAL_ESTATE_USE_MACOS_KEYCHAIN_CA=1
# - Last resort only: disable SSL verification for HTTPS APIs.
# REAL_ESTATE_INSECURE_SSL=0

# Auth mode for HTTP/Docker deployment (default: none).
#   none  — no authentication; use for development or trusted networks
#   oauth — OAuth 2.0; required for Claude Web / ChatGPT Web OAuth inputs
# AUTH_MODE=none

# Legacy client_credentials credentials (Claude Web/Desktop).
# Generate with: openssl rand -hex 16
# OAUTH_CLIENT_ID=your_client_id_here
# OAUTH_CLIENT_SECRET=your_client_secret_here
# OAUTH_TOKEN_TTL=3600

# Public base URL for OAuth metadata discovery (required when AUTH_MODE=oauth).
# Must match the domain Caddy serves, no trailing slash.
# PUBLIC_BASE_URL=https://your-domain.ddns.net

# Auth0 settings (required for ChatGPT Web support).
# Tokens are verified via Auth0 /userinfo endpoint — no client credentials needed here.
# AUTH0_DOMAIN=your-tenant.us.auth0.com   ← Settings → General → Domain (include region, e.g. us, us-5, eu)
# AUTH0_AUDIENCE=https://your-domain.ddns.net/mcp   ← API Identifier (audience)
